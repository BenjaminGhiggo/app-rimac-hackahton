/**
 * Componentes reutilizables de RIMAC
 * Versiones optimizadas para mobile
 */

// Componentes optimizados (recomendado para mobile)
export { OptimizedButton } from './OptimizedButton';
export { OptimizedCard } from './OptimizedCard';
export { OptimizedHeader } from './OptimizedHeader';

// Componentes especializados
export { EmergencyModal } from './EmergencyModal';

// Componentes normales (legacy)
export { Button } from './Button';
export { Card } from './Card';
export { Header } from './Header';

